using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoneyFormation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // foreach (Transform childTransform in transform)
        // {
        //     if ( >3)
        //     {
        //         
        //     }
        //     childTransform.position = new Vector3(childTransform.position.x + , childTransform.position.y + , childTransform.position.z);
        // }
    }
}
